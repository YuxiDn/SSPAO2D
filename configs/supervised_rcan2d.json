{
  "output_dir": "outputs/rcan2d_supervised_probe64_full",
  "model": {
    "name": "rcan2d",
    "in_channels": 1,
    "out_channels": 1,
    "num_features": 64,
    "num_groups": 5,
    "num_blocks": 5,
    "reduction": 8,
    "bn": false,
    "residual_scale": 0.1,
    "final_activation": "sigmoid"
  },
  "data": {
    "patch_size": [
      512,
      512
    ],
    "train": {
      "manifest": "metadata/manifest.csv",
      "augment": true,
      "samples_per_epoch": 2000
    },
    "val": {
      "manifest": "validation/metadata/manifest.csv",
      "augment": false,
      "samples_per_epoch": 400
    },
    "test": {
      "manifest": "Test/metadata/manifest.csv",
      "augment": false
    }
  },
  "training": {
    "epochs": 300,
    "batch_size": 4,
    "num_workers": 4,
    "optimizer": "Adam",
    "initial_learning_rate": 0.0001,
    "lr_scheduler": "ReduceLROnPlateau",
    "factor": 0.5,
    "patience": 10,
    "min_lr": 1e-06,
    "weight_decay": 0.0
  }
}
