{
  "output_dir": "outputs/rcan2d_supervised_v2",
  "model": {
    "name": "rcan2d",
    "in_channels": 1,
    "out_channels": 1,
    "num_features": 32,
    "num_groups": 5,
    "num_blocks": 5,
    "reduction": 8,
    "bn": false,
    "residual_scale": 1.0,
    "final_activation": "none"
  },
  "data": {
    "patch_size": [
      512,
      512
    ],
    "train": {
      "manifest": "metadata/manifest.csv",
      "augment": true,
      "samples_per_epoch": 2000
    },
    "val": {
      "manifest": "validation/metadata/manifest.csv",
      "augment": false,
      "samples_per_epoch": 400
    }
  },
  "training": {
    "epochs": 200,
    "batch_size": 8,
    "num_workers": 4,
    "optimizer": "AdamW",
    "initial_learning_rate": 0.0001,
    "lr_scheduler": "ReduceLROnPlateau",
    "factor": 0.5,
    "patience": 10,
    "min_lr": 1e-06,
    "weight_decay": 0.0
  }
}
