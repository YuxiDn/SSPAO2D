# 0602 Zernike / OTF 分支待办清单

## 总目标

设计一个可解释的像差估计分支：

```text
degraded image y
  ↓
object branch -> x_hat
aberration branch -> Zernike / pupil phase / OTF
  ↓
physics forward model
  ↓
y_hat = h * x_hat
  ↓
compare y_hat with y
```

核心原则：

- 不要一开始依赖 `H ≈ Y / X_hat` 作为主路径，因为 `X_hat` 不准时会严重污染 OTF 估计。
- 先建立稳定的物理 forward model，再逐步加入更可解释的 Zernike 分支和频域证据。
- 频域幅值、相位、功率谱可以作为辅助输入，但不能直接当作 OTF 真值。

## 待办清单

### 1. 建立最小物理 forward model

先实现：

```text
a_k -> pupil phase phi
    -> OTF H
    -> PSF h
    -> y_hat = h * x_hat
```

目标是保证这条链路可微、数值稳定、PSF 归一化。

检查项：

- `PSF h >= 0`
- `sum(h) = 1`
- `H` 满足合理对称性
- 改变 `a_k` 时，PSF 形态有合理变化

### 2. 做 baseline：普通 Zernike coefficient head

先用最简单版本：

```text
y -> encoder -> global pooling -> MLP -> a_k
```

然后：

```text
a_k -> PSF
x_hat * PSF -> y_hat
```

这是所有后续方法的对照组。

它的作用是回答：

```text
普通 FC 直接预测 a_k 到底能做到什么程度？
```

### 3. 加入频域输入，但不做除法

对退化图像做 FFT：

```text
Y = FFT(y)
```

输入 frequency encoder：

```text
log|Y|
cos(angle Y)
sin(angle Y)
PSD = |Y|^2，可选
```

注意：这些只是辅助特征，不是 OTF 真值。

结构：

```text
spatial encoder(y)
FFT encoder([log|Y|, cos(angle Y), sin(angle Y)])
concat
-> aberration latent
-> a_k
```

### 4. 做 `pupil phase phi_hat -> Zernike projection -> a_k`

让网络不要直接输出 `a_k`，而是先输出 pupil phase：

```text
y -> encoder -> phase decoder -> phi_hat(u, v)
```

然后用固定最小二乘投影：

```text
phi_hat ≈ Z a
a = (Z^T W Z + lambda I)^(-1) Z^T W phi_hat
```

这样 `a_k` 来自对 pupil phase 的 Zernike 解释，而不是普通 FC。

加约束：

- remove piston
- `phi_hat` outside pupil = 0
- `L_proj = ||phi_hat - Z a||^2`
- `L_smooth = smoothness(phi_hat)`
- `a_k` bounded

### 5. 加入 fixed pairwise difference layer

在 `phi_hat` 后面加固定几何层：

```text
Delta phi_hat(rho, nu) = phi_hat(rho + nu/2) - phi_hat(rho - nu/2)
```

这一层用于连接 OTF 自相关物理。

后续可以：

```text
Delta phi_hat -> R(nu) = mean exp(i Delta phi_hat)
```

或者：

```text
Delta phi_hat -> Zernike difference projection -> a_k
```

目标是让网络中间表示更贴近 OTF 真正感受到的 pairwise phase difference。

### 6. 构建 Zernike response dictionary / template bank

预计算每个 Zernike mode 的物理响应模板：

```text
mode k with +epsilon -> H_k+
mode k with -epsilon -> H_k-
```

模板可以包括：

- OTF 幅值响应：`Delta log amplitude`
- OTF 相位响应：`phase response`
- PSF pattern
- radial / angular frequency response

得到：

```text
template bank T_k
```

作用是让网络知道：

```text
每个 Zernike 模式会怎样影响频谱 / OTF / PSF
```

### 7. 做 template attention 分支

从图像 FFT 得到 frequency feature：

```text
F_img = FFT encoder([log|Y|, cos(angle Y), sin(angle Y)])
```

和每个 Zernike response template 做匹配：

```text
score_k = similarity(F_img, T_k)
```

输出每个 mode 的 attention score：

```text
defocus score
astigmatism score
coma score
spherical score
...
```

注意不要直接用 softmax 当系数。建议：

```text
importance_k = sigmoid(score_k)
signed_k = tanh(signed_head)
a_template_k = max_amp_k * importance_k * signed_k
```

### 8. 融合两条 Zernike 证据

形成两个估计：

```text
a_phase     来自 phi_hat -> Zernike projection
a_template  来自 template attention
```

融合：

```text
a = a_phase + gate * a_template
```

或者更保守：

```text
a = weighted_fusion(a_phase, a_template)
```

其中 `gate` 表示频域模板证据可信度。

### 9. 暂时不要使用强化学习

当前问题是连续可微的物理反演：

```text
a_k, phi, OTF, PSF, convolution
```

都可以通过反向传播训练。强化学习会增加训练难度，不是第一优先级。

### 10. 训练策略

推荐分阶段：

```text
Stage 1: synthetic data pretrain
         已知 x, a_k, h

Stage 2: train object branch + coefficient baseline

Stage 3: 加入 phi_hat projection branch

Stage 4: 加入 FFT features

Stage 5: 加入 template attention

Stage 6: real data fine-tuning
```

如果有多帧数据，尽量用：

```text
y_i = h_i * x
```

共享同一个 object `x`，不同帧预测不同 `h_i`，这能显著降低 object / PSF 互相作弊。

## 推荐最终结构

```text
degraded image y
  |
  |-- spatial encoder
  |
  |-- FFT encoder
  |      input: log|Y|, cos(angle Y), sin(angle Y), optional PSD
  |
  |-- fused feature
         |
         |-- object branch
         |      -> x_hat
         |
         |-- phase branch
         |      -> phi_hat on pupil
         |      -> Zernike projection
         |      -> a_phase
         |
         |-- template attention branch
                -> compare FFT feature with Zernike templates
                -> a_template

a = a_phase + gate * a_template
  ↓
Zernike physics layer
  ↓
OTF / PSF h
  ↓
y_hat = h * x_hat
  ↓
loss(y_hat, y)
```

## 一句话版本

先做普通 `a_k` baseline；再让网络输出 pupil phase 并投影到 Zernike；再加入 FFT 幅相特征；最后用预计算的 Zernike response templates 做 attention 匹配。整个过程不要依赖 `Y / X_hat` 作为主路径，而是用可微物理 forward model 保证 `x_hat` 和像差估计能共同解释退化图像。
