# 0602 Zernike / OTF 分支待办清单（结果对齐修正版）

## 总目标

设计一个可解释的像差估计分支：

```text
degraded image y
  ↓
object branch -> x_hat
aberration branch -> Zernike / pupil phase / OTF
  ↓
physics forward model
  ↓
y_hat = h * x_hat
  ↓
compare y_hat with y
```

核心原则：

- 不把 `H ≈ Y / X_hat` 作为主路径。`X_hat` 不准时，这条路会把 object 误差污染进 OTF。
- 先把 forward model、评估指标、baseline 对照做扎实，再推进更复杂的 `phi_hat`、pairwise phase difference、template attention。
- 频域幅值、相位、功率谱只能作为辅助证据，不能当作 OTF 真值。
- 现有结果已经说明：只优化重投影不等于 object 和 Zernike 都正确；后续待办必须围绕“可辨识性”和“可比较评估”推进。

## 已有模型结果对待办的修正依据

### 1. `abesplit2d_self_supervised`

配置特点：

```text
adversarial_weight = 0.001
coeff_l2 = 1e-4
final_activation = sigmoid
fft = true
```

验证记录只包含重投影指标：

```text
best cycle_psnr ≈ 43.14 @ 72000 iter
final cycle_psnr ≈ 42.96 @ 75500 iter
final cycle_ssim ≈ 0.9846
```

结论：

- 重投影链路能训练到很高 PSNR，说明现有 forward model 和卷积闭环基本可用。
- 但该实验没有记录 `object_psnr/object_ssim/zernike_mae`，不能证明 object branch 或 Zernike branch 正确。
- 因此第一优先级不是继续堆新结构，而是补齐可比较评估。

### 2. `abesplit2d_self_supervised_adv1_nocoeffl2`

配置特点：

```text
adversarial_weight = 1.0
coeff_l2 = 0
final_activation = sigmoid
fft = true
```

关键验证结果：

```text
best cycle_psnr    ≈ 34.62 @ 31500 iter
final cycle_psnr   ≈ 30.08 @ 300000 iter
best object_psnr   ≈ 21.75 @ 113500 iter
final object_psnr  ≈ 21.70
best zernike_mae   ≈ 0.129 @ 2000 iter
final zernike_mae  ≈ 0.140
```

结论：

- `adversarial_weight = 1.0` 明显过强，重投影质量从早期峰值下滑到约 30 dB。
- object 质量长期停在约 21.7 dB，说明真实图像重投影和 synthetic object 约束还不足以稳定恢复 clean object。
- Zernike MAE 早期最低，随后变差，说明系数分支容易被后续 object / GAN / cycle 目标带偏。
- 去掉 `coeff_l2` 没有带来明确收益；至少需要恢复轻量约束或改成有界输出。

### 3. `abesplit2d_self_supervised_random_gpus45`

输出位置：

```text
/nfs/data38/yxdeng993/output/SSPAO2D/abesplit2d_self_supervised_random_gpus45
```

配置特点：

```text
adversarial_weight = 0.1
coeff_l2 = 0
final_activation = null
random_reaberration_object_weight = 5.0
random_reaberration_feature_weight = 0.0
random_reaberration_coeff_weight = 5.0
phase_loss_weight = 10.0
phase_grad_loss_weight = 10.0
batch_size = 4
accumulate_steps = 4
已记录到 41000 iter / 82 epochs
```

关键验证结果：

```text
best object_psnr  ≈ 17.73 @ 500 iter
best score        ≈ 16.94 @ 500 iter
best cycle_psnr   ≈ 42.75 @ 31500 iter
best zernike_mae  ≈ 0.1266 @ 40000 iter
final cycle_psnr  ≈ 40.85 @ 41000 iter
final object_psnr ≈ 9.84  @ 41000 iter
final zernike_mae ≈ 0.1267 @ 41000 iter
```

训练尾部指标显示：

```text
train cycle_psnr ≈ 41.9
train loss_phase ≈ 8.26
train loss_phase_grad ≈ 2.67
train random_reaberration_object_loss ≈ 0.93
train random_reaberration_coeff_loss ≈ 0.59
```

结论：

- 随机像差一致性首组配置没有解决 object 锚定，反而让验证 `object_psnr` 从第 500 iter 的约 17.7 dB 继续退化到约 9.8 dB。
- `zernike_mae` 稳定到约 0.1266，略好于 `adv1_nocoeffl2` 的约 0.129，但代价是 object 明显崩坏。
- 重投影仍能到 40 dB 以上，说明模型在用错误的 `x_hat/a_hat` 组合解释 `y`，可辨识性问题更严重。
- 该实验不应继续作为当前主线跑满 300k；应先降随机一致性权重、降 phase 权重或改训练阶段。

## 修正后的待办清单

### P0. 先统一评估口径

目标：所有实验都必须能回答三个问题：

```text
1. y_hat 是否能重投影回 y？
2. x_hat 是否接近 clean object？
3. a_hat 是否接近真实 Zernike？
```

检查项：

- 所有 self-supervised 实验都记录：
  - `cycle_psnr`, `cycle_ssim`
  - `object_psnr`, `object_ssim`, `object_rmse`, `object_pcc`
  - `zernike_mae`, `zernike_rmse`
  - `random_reaberration_object_loss`, `random_reaberration_coeff_loss`
- 对 `abesplit2d_self_supervised` 补一次同口径验证，不能只看 cycle 指标。
- checkpoint 选择不要只按 `cycle_psnr`，建议用：

```text
score =
  object_psnr
- alpha * zernike_mae
+ beta  * cycle_psnr
```

先取：

```text
alpha = 20
beta = 0.05
```

并同时保留 `best_object`, `best_zernike`, `best_cycle`, `latest` 四类 checkpoint 结果。

### P0. 修正随机像差一致性策略

首组 random 实验已经给出负面结论：当前权重组合会破坏 object branch。下一步不是继续加大随机一致性，而是把它改成弱约束或后期约束。

立即停止使用以下组合做长训：

```text
adversarial_weight = 0.1
coeff_l2 = 0
final_activation = null
random_reaberration_object_weight = 5.0
random_reaberration_feature_weight = 0.0
random_reaberration_coeff_weight = 5.0
phase_loss_weight = 10.0
phase_grad_loss_weight = 10.0
```

下一组建议：

```text
adversarial_weight = 0.001 或 0
coeff_l2 = 1e-4
final_activation = null
phase_loss_weight = 1.0
phase_grad_loss_weight = 1.0
random_reaberration_object_weight = 0.5
random_reaberration_feature_weight = 0.0
random_reaberration_coeff_weight = 1.0
```

训练策略：

- 前 5k-10k iteration 关闭 random re-aberration，只训练 synthetic object / zernike / identity / real reprojection。
- `object_psnr` 不再上升后，再打开弱 random consistency。
- 如果 `object_psnr` 低于 18 dB 或连续 3 个 checkpoint 下降，直接停掉 random 分支。
- checkpoint 选择必须优先看 `object_psnr`，不能按 `cycle_psnr` 或 `zernike_mae` 选择。

判断标准：

- 新配置必须先超过 random 首组的 `best object_psnr ≈ 17.73`。
- 合格线仍然是超过 `adv1_nocoeffl2` 的 `best object_psnr ≈ 21.75`。
- `zernike_mae` 只有在 object 不崩的前提下才有意义；单独追求 `0.126` 左右的 MAE 不值得。

### P1. 重做最小 ablation baseline

当前已有 `abesplit2d`，所以“普通 Zernike coefficient head”不需要从零设计；要做的是控制变量对照。

必跑对照：

```text
A. no GAN / coeff_l2=1e-4 / fft=true
B. adv=0.001 / coeff_l2=1e-4 / fft=true
C. adv=0.001 / coeff_l2=1e-4 / fft=true / weak random consistency on
D. adv=0 / coeff_l2=1e-4 / fft=true / weak random consistency on
E. adv=0.001 / coeff_l2=1e-4 / fft=false / weak random consistency on
```

每组只需要先跑到足够看趋势：

```text
50k ~ 100k iterations
```

停止条件：

- `cycle_psnr` 持续下降且 object/zernike 不提升，提前停。
- `zernike_mae` 早期达到低点后持续恶化，保存早期 best checkpoint，用于后续可视化分析。
- `object_psnr` 低于 18 dB 或出现 random 首组那种单调退化，提前停。

### P1. 验证并冻结物理 forward model

现有 `AO2DForwardModel` 和 `generate_psf2d_from_zernike` 已经能支持训练；这里不是重新实现，而是补 sanity test。

必须检查：

- `PSF h >= 0`
- `sum(h) = 1`
- `coeff = 0` 时 PSF 合理且稳定
- 改变 defocus / astigmatism / coma 时，PSF 形态有可解释变化
- 同一 `x_hat, a_hat` 的 `forward(x_hat, a_hat)` 与训练日志里的 cycle 指标一致

完成后把 forward model 视为冻结组件，避免在模型 ablation 期间同时改光学链路。

### P1. 解决 Zernike 分支早期好、后期差的问题

已有结果显示 `zernike_mae` 在第 2000 iteration 最好，后面被其他目标带偏。

优先尝试：

- 恢复轻量 `coeff_l2 = 1e-4` 或改成 `a_k` 有界输出。
- 对 `zernike_coeff_l1_weight` 做 warmup / decay，不让早期 coefficient anchor 被后期 GAN 和 cycle 吞掉。
- 单独记录 synthetic object 分支上的 `zernike_mae`，区分“合成像差学不会”和“真实图像不可辨识”。
- 对每个 Zernike mode 输出 MAE，不只看平均值；优先确认 defocus、astigmatism、coma 是否被混淆。

### P1. 解决 object branch 锚定不足

已有 `adv1_nocoeffl2` 的 object PSNR 长期只有约 21.7 dB。

优先尝试：

- 降低或关闭 GAN，先恢复物理一致性和 object 质量。
- 保持 `cycle_object_weight = 60.0`、`cycle_object_feature_weight = 100.0`，不要在 object 还差时加大随机一致性。
- 检查 `final_activation = sigmoid` 是否压缩动态范围；随机一致性实验优先用 `final_activation = null`。
- 对保存图做固定样本对比：`input y / x_hat / clean object / y_hat / residual`。

### P2. 频域输入只做辅助证据

现有模型已经使用 FFT 幅值分支，但还没有证明频域分支能稳定提升 Zernike。

下一步不是 `Y / X_hat`，而是补充更稳的频域特征：

```text
log|Y|
normalized log|Y|
cos(angle Y)
sin(angle Y)
radial power spectrum
angular power bins
```

对照方式：

```text
fft=false
log amplitude only
log amplitude + phase sin/cos
log amplitude + radial/angular statistics
```

只有当 P0/P1 的 baseline 指标稳定后，再判断频域输入是否真有收益。

### P2. `pupil phase phi_hat -> Zernike projection`

该项仍然有价值，但应放在 baseline 稳定之后。

结构：

```text
y -> encoder -> phase decoder -> phi_hat(u, v)
phi_hat ≈ Z a
a = (Z^T W Z + lambda I)^(-1) Z^T W phi_hat
```

约束：

- remove piston
- `phi_hat` outside pupil = 0
- `L_proj = ||phi_hat - Z a||^2`
- `L_smooth = smoothness(phi_hat)`
- `a_k` bounded

进入条件：

- 已有 coefficient baseline 在同口径评估下稳定。
- random re-aberration 已经证明能或不能改善 object/zernike。
- 每个 mode 的误差分析显示 FC head 存在明确结构性混淆。

### P3. fixed pairwise difference layer

该层用于让中间表示更贴近 OTF 真正感受到的 pairwise phase difference：

```text
Delta phi_hat(rho, nu) = phi_hat(rho + nu/2) - phi_hat(rho - nu/2)
```

后续可以：

```text
Delta phi_hat -> R(nu) = mean exp(i Delta phi_hat)
```

或：

```text
Delta phi_hat -> Zernike difference projection -> a_k
```

暂时不要在 baseline 未稳定前实现，否则很难判断收益来自物理结构还是训练配置。

### P3. Zernike response dictionary / template bank

预计算每个 Zernike mode 的物理响应模板：

```text
mode k with +epsilon -> H_k+
mode k with -epsilon -> H_k-
```

模板包括：

- OTF 幅值响应：`Delta log amplitude`
- OTF 相位响应：`phase response`
- PSF pattern
- radial / angular frequency response

该项应作为可视化和诊断工具先做，不要立刻接入训练。

### P4. template attention 分支

只有在 template bank 对真实误差模式有解释力后，再接入网络：

```text
F_img = FFT encoder([log|Y|, cos(angle Y), sin(angle Y)])
score_k = similarity(F_img, T_k)
```

不要直接用 softmax 当系数。建议：

```text
importance_k = sigmoid(score_k)
signed_k = tanh(signed_head)
a_template_k = max_amp_k * importance_k * signed_k
```

融合方式：

```text
a = a_phase + gate * a_template
```

或更保守：

```text
a = weighted_fusion(a_phase, a_template)
```

#### 2026-06-03 template attention presence-only 修正记录

这次修正的核心不是让所有 mode 都由 template attention 直接回归完整系数，而是把它拆成两类作用：

```text
signed mode:      template attention 可以输出 signed coefficient
presence-only:    template attention 只判断该 mode 是否存在，再用 presence * base_head 门控主干回归
```

当前 `signed_zernike_indices = [6, 7, 8, 9, 15]`。也就是说：

- `j=6,7,8,9,15` 仍然让 template attention 判断正负和幅值，loss 里有 coefficient L1。
- `j=3,4,5,10,11,12,13,14` 不再让 template attention 决定正负；这些 mode 的正负交给 `base_head(features)`，template attention 只输出 `presence`。

为什么要这样改：

- 从 template buffer 可视化看，不少 mode 的 `+eps` 和 `-eps` OTF template 很像，中心差分 `scores_pos - scores_neg` 接近 0。
- 如果这类 mode 仍然用 `sign = tanh(response_scale * (scores_pos - scores_neg))` 判断正负，网络很容易把接近 0 的差分当成随机符号。
- 因此对符号不可靠的 mode，template attention 不应直接给 signed coefficient，只应告诉主干“这个 mode 的频域响应是否存在”。

对应代码变化：

```text
src/ao2d/models/zernike_template_attention.py

a_base = base_head(features)
a_template = signed_mode ? max_amp * presence * sign : max_amp * presence
template_proposal = signed_mode ? a_template : presence * a_base
a_final = confidence * template_proposal + (1 - confidence) * a_base
```

这里最容易误解的是：对 `j=3,4,5,10,11,12,13,14`，`a_template = max_amp * presence` 只是 attention head 内部的模板幅值，不应当被当成最终系数解释。最终前向实际使用的是 `presence * a_base`。

pretrain loss 也同步改了：

```text
scripts/pretrain_template_attention.py

signed modes:
  L_coeff = L1(pred, target)

presence-only modes:
  不再使用 L1(abs(pred), abs(target))
  只保留 presence BCE
```

旧 loss 的问题是，对于 presence-only mode，`pred = max_amp * presence`。如果继续用 `L1(abs(pred), abs(target))`，最优解会把 `presence` 拉到 `abs(target) / max_amp` 附近。例如 `abs(target)=0.1 um`、`max_amp=0.7 um` 时，presence 会被拉到约 `0.14`；但 BCE 又希望 active mode 的 presence 接近 1。这两个目标互相冲突，所以旧训练里 presence 很低。

修正后短跑结果：

```text
output_dir = /data/yxdeng993/output/SSPAO2D/pretrain_template_attention_depthwise_presenceonly_e005_b128_8obj_32ch_4blk_100ep

epoch 1: val_signed_mae=0.12540, val_sign=0.479, val_presence=0.843
epoch 6: val_signed_mae=0.12485, val_sign=0.519, val_presence=0.843
epoch 7: val_signed_mae=0.12470, val_sign=0.522, val_presence=0.843
```

这个结果说明：

- presence 已经从旧逻辑的低值回到约 `0.84`，说明 loss 冲突被消掉了。
- signed mode 的符号判断还没有学好，`val_sign` 只有约 `0.52`，接近随机略高。
- `val_signed_mae` 约 `0.125`，说明 `j=6,7,8,9,15` 的 signed coefficient 仍然没有被 template attention 有效回归出来。

当前 validation 可视化中 `j=6,7,8,9` 仍然很小的原因：

```text
epoch 6 validation figure samples, mean abs values

j   target_abs  pred_abs  presence  sign_mean
6   0.0786      0.0025    0.8217    -0.0011
7   0.1924      0.0020    0.8169    -0.0029
8   0.1670      0.0047    0.8186     0.0095
9   0.1163      0.0020    0.8206    -0.0016
15  0.0988      0.0008    0.7384    -0.0019
```

这些 mode 的 `presence` 并不小，真正小的是 `sign`。因为 signed coefficient 是：

```text
pred = max_amp * presence * sign
```

所以当 `sign ≈ 0` 时，即使 `presence ≈ 0.8`，最终 `pred_abs` 仍会接近 0。这不是 presence-only 修正导致的，而是 signed template 的 `+eps/-eps` 可分性或 encoder 训练还没有解决。

同时，当前 synthetic 采样使用：

```text
zernike_spectrum = loworder
coeff = coeff / radial_order
```

因此高一些的 radial order 本来就会被采得更小；判断 `j=6,7,8,9` 是否异常，不能只看图上柱子小，要同时看 `target_abs`、`pred_abs`、`presence` 和 `sign`。

下一步判断标准：

- presence-only mode 不再用 `prediction` 柱状图判断系数是否正确；应看 `presence` 是否能识别 active mode，以及最终融合后 `presence * base_head` 的 per-mode MAE。
- signed mode 才看 `prediction` 是否接近 target；当前 `j=6,7,8,9,15` 的主要问题是 `sign` 没学出来。
- 如果继续只靠 template attention 训练 signed mode，可以尝试增大 signed coefficient loss 权重、提高 signed mode 采样幅度或改成 flat spectrum 做诊断。
- 更保守的训练路线是：template attention 只做 presence，所有 mode 的正负和幅值都交给 base/head，再看这个 frequency prior 是否能改善主模型。

### 暂时不要使用强化学习

当前问题是连续可微的物理反演：

```text
a_k, phi, OTF, PSF, convolution
```

都可以通过反向传播训练。强化学习会增加训练难度，不是当前优先级。

## 近期执行顺序

```text
1. 补齐同口径评估和 checkpoint 选择逻辑
2. 对已有 abesplit2d_self_supervised 做 object/zernike 验证
3. 归档 random re-aberration 首组负面结论，不再重复强 random 配置
4. 跑 P1 ablation：GAN、coeff_l2、FFT、final_activation
5. 降权重重跑 weak random consistency，不再使用首组强 random 配置
6. 做 per-mode Zernike 误差分析和固定样本可视化
7. 根据结果决定是否进入 phi_hat projection
8. 再考虑 pairwise difference 和 template attention
```
